# Custom Implementation Guides

Place custom FHIR Implementation Guide packages (`.tgz` or `.tar.gz`) in this directory.

The Docker Compose setup mounts this folder into the Inferno validator wrapper at `/igs`.
